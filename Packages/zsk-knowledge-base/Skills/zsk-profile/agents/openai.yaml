interface:
  display_name: "ZSK Profile"
  short_description: "维护有主体证据的三层 Profile 资产"
  default_prompt: "仅由 $zsk-router 后台调用：按主体证据整理 05 Profile，区分确认事实、运营设定和候选素材，不绕过来源身份校验。"
policy:
  allow_implicit_invocation: false
