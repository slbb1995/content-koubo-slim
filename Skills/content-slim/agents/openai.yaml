interface:
  display_name: "Content Slim 内容生产"
  short_description: "单一入口管理口播内容任务、版本状态与三次真人确认流程"
  default_prompt: >-
    使用 $content-slim 复用已确认的本地内容绑定开始或继续同一个口播 Run；首次缺少绑定时先只读预检明确路径并等我确认，绑定完成后仍须在方向、正文和配套三个真人确认点分别停下。
